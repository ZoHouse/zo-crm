# LiveKit Agents SDK
# Using Deepgram STT (free tier) + OpenAI TTS (cheap) instead of Cartesia
livekit-agents[deepgram,silero,openai]

# Supabase for CRM data
supabase>=2.0.0

# Other dependencies
python-dotenv>=1.0.0
httpx>=0.27.0
