{
  "name": "smart-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @smart-crm/db generate",
    "db:migrate": "pnpm --filter @smart-crm/db migrate",
    "db:seed": "pnpm --filter @smart-crm/db seed"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.90.1",
    "better-sqlite3": "^11.6.0",
    "dotenv": "^17.2.3",
    "tsx": "^4.19.0",
    "turbo": "^2.3.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
